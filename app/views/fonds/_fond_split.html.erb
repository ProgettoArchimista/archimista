<%= stylesheet_link_tag "fonds_modal" %>
<div class="modal hide fade" id="split-fonds-modal">
  <div class="modal-header">
    <a class="close" data-dismiss="modal">×</a>
    <h3>Dividi complessi archivistici</h3>
  </div>
  <div class="modal-body">
    <p>Dividi il complesso <strong><%= object.name %></strong> con uno di tua scelta.</p>
    <p class="muted">Il complesso sarà estratto dal primo livello di appartenenza creando un nuovo complesso di livello 1</p>
<%# TAI aggiunto = prima di form_tag %>
    <%= form_tag({:controller => 'fonds', :action => 'split', :id => object.id}, {:id => "split-fonds-form", :class => "skip-prompt"}) do %>
      <div  class="dropdown open">
        <%= render :partial => 'fonds/jumpsplit_to', :locals => {:fond => object, :root => object} %>
      </div>
      <input name="new_root_id" value="" type="hidden" />
    <% end %>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal"><%= t('cancel') %></button>
    <button id="confirm-split" class="submit btn btn-primary disabled">Dividi</button>
  </div>
</div>
