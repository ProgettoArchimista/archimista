<!DOCTYPE html>
<html lang="<%= I18n.default_locale.to_s %>">
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title><%= APP_NAME %></title>
    <%= default_stylesheets %>
    <style type="text/css">
      #login-header-image-area
      {
        margin-top: 10px;
        margin-bottom: 30px;
        background-color: #B9CDE5;
        text-align: right;
        width: 100%;
        height: 140px;
      }
      #login-header-image-area img
      {
        height: 140px;
        margin-right: 30px;
      }
      #login
      {
        padding-top: 0px;
        background: #f0f0f0;
      }
      #login-form
      {
        width: 300px;
        margin: 0 auto;
        padding: 18px 30px 11px 30px;
        border: 2px solid #ccc;
        -webkit-border-radius: 4px;
        -moz-border-radius: 4px;
        border-radius: 4px;
        background: #fff;
      }
      #login-form h1
      {
        margin: 0 0 20px 0;
        border-bottom: 1px solid #ccc;
        font-size: 24px;
        line-height: 36px;
      }
      #login-form label.checkbox
      {
        font-size: 11px;
      }
      #login-form input[type="submit"]
      {
        margin-top: 20px;
      }
      #login-form .alert-success
      {
        position: static;
        width: auto;
        margin-top: 0;
        margin-left: 0;
        background-color: #B9CDE5;
        border-color: #90b0c0;
        color: #004488;
      }
    </style>
  </head>

  <body id="login">
    <div id="login-header-image-area">
      <img src="/images/archheader1.jpg"/>
    </div>

    <div id="login-form">
        <%= yield %>
    </div>
  </body>
</html>

