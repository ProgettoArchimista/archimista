xml.catheader do
  xml.systemId "#{PROVIDER}"
  xml.contact do
    xml.name "INGLOBA360"
    xml.mail "account@ingloba360.it"
  end
  xml.filedesc do
    xml.title "Export da Archimista a SAN"
    xml.date DateTime.now
  end
end